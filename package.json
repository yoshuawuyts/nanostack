{
  "name": "nanostack",
  "description": "Small middleware stack library",
  "repository": "yoshuawuyts/nanostack",
  "version": "1.0.2",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps && nyc node test.js",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov"
  },
  "dependencies": {},
  "devDependencies": {
    "dependency-check": "^2.7.0",
    "nyc": "^10.1.2",
    "standard": "^8.6.0",
    "tape": "^4.6.3"
  },
  "keywords": [
    "messagebus",
    "message",
    "bus"
  ]
}
